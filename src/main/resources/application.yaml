spring:
  datasource:
    url: jdbc:mysql://localhost:3306/realstateapp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true  # allowPublicKeyRetrieval ??
    username: root
    password: 1502489awdsa!!@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  p6spy:
    logging:
      log: com.p6spy.engine.spy.appender.StdoutLogger
